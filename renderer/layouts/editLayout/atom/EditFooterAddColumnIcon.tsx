import { TableRows } from '@mui/icons-material';

export const EditFooterAddColumnIcon = () => {
	return (
		<TableRows
			titleAccess="カラムを追加"
			fontSize="small"
			sx={{
				cursor: 'pointer',
			}}
		/>
	);
};
