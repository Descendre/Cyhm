import { LibraryAdd } from '@mui/icons-material';

export const EditFooterAddTableIcon = () => {
	return (
		<LibraryAdd
			titleAccess="テーブルを追加"
			fontSize="small"
			sx={{
				cursor: 'pointer',
			}}
		/>
	);
};
