import { Apps } from '@mui/icons-material';

export const TopProjectHeaderViewModeIcon = () => {
	return (
		<Apps
			fontSize="small"
			titleAccess="グリッドで表示"
			sx={{
				cursor: 'pointer',
			}}
		/>
	);
};
