import { FilterList } from '@mui/icons-material';

export const TopProjectHeaderSortIcon = () => {
	return (
		<FilterList
			fontSize="small"
			titleAccess="ソート"
			sx={{
				cursor: 'pointer',
			}}
		/>
	);
};
